// VideoSDK configuration.
//
// Paste your VideoSDK token below. This is for local testing only. In
// production, create the ingest session on your backend and never ship the
// token to the browser (see the README).
window.CONFIG = {
  // Required: your VideoSDK auth token.
  token: "",

  // Optional default (you can also fill this in the UI).
  roomId: "",
};
